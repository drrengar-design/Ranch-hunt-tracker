[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Function custom path /api/sync is matched before this SPA fallback.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
